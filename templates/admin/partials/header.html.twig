<header>  
<div class="bloc_nav">
<nav class="row navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
        <a class="navbar-brand" href="{{ path('admin_dashboard_index') }}"><img class="icon_ailesx" src="{{asset('img/nav/logo_ailesx.png')}}" alt=""></a>
        {% if controller_page is not defined %}
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_dashboard_index') }}">
                    <i class="fas fa-chart-line"></i>
                    DASHBOARD
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_flights_index') }}">
                    <i class="fas fa-plane-departure"></i>
                    VOLS
                    </a>
                </li>
                 <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_bookings_index') }}">
                    <i class="fas fa-folder"></i>
                    RESERVATIONS
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_destination_index') }}">
                    <i class="fas fa-map-marker-alt"></i>
                    DESTINATIONS
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_users_index') }}">
                    <i class="fas fa-users"></i>
                    UTILISATEURS
                    </a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                <!-- <form class="form-inline my-2 my-lg-0">
                    <button class="btn btn-secondary my-2 my-sm-0" type="submit">OK</button>
                    <input class="form-control mr-sm-2" type="text" placeholder="Search">
                </form> -->
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle menu-account" data-toggle="dropdown" id="accountDropdownLink">
                        <img src="{{ app.user.picture }}" alt="Avatar de {{ app.user.fullName }}" class="avatar avatar-mini">{{ app.user.fullName }}
                    </a>
    
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="{{ path('home') }}" class="dropdown-item">
                        <i class="fas fa-eye"></i>
                        Voir le site
                        </a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('admin_account_preLogout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            </ul>
        </div>
        {% endif %}
    </nav>
    {% for label,messages in app.flashes %}
        <div class="container" style="margin-top:4rem;">
            <div class="alert alert-{{ label }}">
                {% for message in messages %}
                <p>{{ message | raw }}</p>
                {% endfor %}
            </div>
        </div>
    {% endfor %}
</div>
        <div class="screen fixed-bottom" style="font-size:50px; color:blue;bottom:26px;"></div>
</header>